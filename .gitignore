/target
**/*.rs.bk
Cargo.lock.bak
.DS_Store

# Local agent/editor client config
.claude/
.cursor/

# Python / ml_pipeline
ml_pipeline/.venv/
ml_pipeline/artifacts/
ml_pipeline/**/__pycache__/
ml_pipeline/**/*.egg-info/
ml_pipeline/.pytest_cache/
ml_pipeline/.coverage
ml_pipeline/htmlcov/

# datasets (large, fetched via scripts/fetch_datasets.sh) — track the card, not the corpora
/data/*
!/data/DATA_CARD.md

# secrets — API keys for distillation teachers (OpenRouter / Vercel AI Gateway)
.env
*.env
!*.env.example
