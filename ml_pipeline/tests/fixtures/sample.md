# Sample Markdown

This document exists to exercise the prose path of the labeler. Markdown has
no AST, so all weight should fall on `y_semantic`.

- Bullet one explains something.
- Bullet two restates bullet one in slightly different words.

The compression target removes redundant restatement while keeping the
substantive claim intact.
